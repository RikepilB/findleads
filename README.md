# findleads

> **The repo is the prompt.** This README states the product intent; `CLAUDE.md` /
> `AGENTS.md` state how agents operate; `docs/` holds architecture + decisions; `tests/`
> define expected behavior; `.github/` carries task + review context. A well-structured
> repo produces better planning, better code, and repeatability.

## What it does
_TODO: one paragraph on the product intent, the user, and the core value._

## Stack
_TODO: languages, frameworks, services._

## Setup
1. Copy `.env.example` → `.env` and fill the values.
2. Install dependencies.
3. Start the dev server.

## Key workflows
- _TODO_

## Map
- `CLAUDE.md` / `AGENTS.md` — how AI agents (Claude Code / Codex) operate here
- `docs/architecture.md` — system patterns + trade-offs
- `docs/decisions.md` — decision log (ADRs)
- `docs/handoff/HANDOFF.md` — append-only session handoff tree
- `.claude/` — agents, rules (guardrails), commands, hooks (committed: it is part of the prompt)
