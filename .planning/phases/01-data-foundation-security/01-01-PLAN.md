---
phase: 01-data-foundation-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - next-env.d.ts
  - eslint.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - public/
  - vitest.config.ts
  - tests/unit/smoke.test.ts
autonomous: false
requirements: [SEC-01]
must_haves:
  truths:
    - "The project has a working local dev/build/lint/typecheck/test pipeline matching .github/workflows/ci.yml's lint -> typecheck -> test -> build sequence"
    - "Existing repo scaffold files (README.md, AGENTS.md, .gitignore, .github/, .claude/, docs/, tests/ layout) are preserved untouched by the Next.js scaffold merge"
  artifacts:
    - path: "package.json"
      provides: "pnpm scripts: dev, build, lint, typecheck, test"
    - path: "vitest.config.ts"
      provides: "Vitest config wired to tsconfig path aliases, node test environment"
    - path: "tests/unit/smoke.test.ts"
      provides: "Trivial passing test so `pnpm test -- --run` is green from Wave 1 onward"
  key_links:
    - from: "package.json test script"
      to: "vitest.config.ts"
      via: "\"test\": \"vitest\" invoked as `pnpm test -- --run`"
      pattern: "\"test\":\\s*\"vitest\""
---

<objective>
Bootstrap the Next.js App Router project into this already-scaffolded (but code-free) repo root,
without disturbing the existing `project-scaffold` artifacts (README.md, AGENTS.md, .gitignore,
.github/, .claude/, docs/, tests/), and stand up the pnpm script surface (`dev`/`build`/`lint`/
`typecheck`/`test`) that `.github/workflows/ci.yml` already expects. Install this phase's two
flagged-`[SUS]` packages (`server-only`, `vitest`) only after an explicit human legitimacy check,
per this project's package-legitimacy gate.

Purpose: every later plan in this phase (secrets, schema, DAL, tests) needs a working
`package.json`/`tsconfig.json`/test runner to build on top of — this is Wave 1, the foundation
every other wave depends on.
Output: a booting Next.js app, `pnpm lint`/`pnpm typecheck`/`pnpm build`/`pnpm test -- --run` all
green, with none of Phase 1's real dependencies (Drizzle, Neon, Zod) installed yet — those land in
later waves once secrets/schema exist to use them meaningfully.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-security/01-RESEARCH.md
@.planning/phases/01-data-foundation-security/01-SKELETON.md

<interfaces>
<!-- vitest.config.ts target shape for Task 3. No prior code exists yet in this repo — this is
     the exact config shape to create, not an extraction from existing code. -->

```typescript
// vitest.config.ts
import { defineConfig } from 'vitest/config'
import tsconfigPaths from 'vite-tsconfig-paths'

export default defineConfig({
  plugins: [tsconfigPaths()],
  test: {
    environment: 'node',
  },
})
```

Plan 01-02 will edit this file to add a `test.env` block (dummy `PLACES_API_KEY`, test-branch
`DATABASE_URL`) once secrets exist — do not add env handling in this plan.
</interfaces>
</context>

## Phase Goal

**As a** developer building findleads, **I want to** have a durable database schema and secured
Places API key handling in place, **so that** every later scrape, job, and CRM feature has
trustworthy storage and security to build on.

(See `01-SKELETON.md` "Capability Proven End-to-End" for why this phase has no user-facing UI
slice — Phase 1's own REQUIREMENTS.md IDs are all schema/security, zero UI, and ROADMAP.md
explicitly designates Phases 1-2 as backend-only groundwork.)

## Artifacts This Phase Produces (this plan's share)

- `package.json` — pnpm scripts `dev`, `build`, `lint`, `typecheck`, `test`; Next.js/React deps
  from `create-next-app`; no Phase 1 runtime deps yet (those install here too, in Task 3)
- `tsconfig.json`, `next.config.ts`, `next-env.d.ts`, `eslint.config.mjs` — framework defaults
- `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `public/` — framework scaffold (unmodified
  from `create-next-app` output; no Phase 1 code lives here)
- `vitest.config.ts` — Vitest config, `vite-tsconfig-paths` plugin, `node` test environment
- `tests/unit/smoke.test.ts` — trivial passing test proving the runner works

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js into a sibling temp dir, merge framework files into repo root</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, next-env.d.ts, eslint.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, public/</files>
  <read_first>.planning/phases/01-data-foundation-security/01-RESEARCH.md (bootstrap_sequence section, Pitfall 3)</read_first>
  <action>
    The repo root is not empty (README.md, AGENTS.md, .gitignore, .github/, .claude/, docs/,
    tests/, LICENSE, CLAUDE.local.md, opencode.json, .env, .env.example, .mcp.json already exist
    from an earlier project-scaffold run) — create-next-app refuses to run directly against a
    directory outside its small allowlist. Run
    `pnpm create next-app ../findleads-scaffold-tmp --ts --app --use-pnpm --eslint --import-alias "@/*" --yes`
    to scaffold into a sibling temporary directory instead of the repo root. From that temp
    directory, copy only the framework-generated files into the repo root: package.json,
    tsconfig.json, next.config.ts, next-env.d.ts, the ESLint config file, any Tailwind/PostCSS
    config file the generator produced, the app/ directory, and public/. Do not copy README.md,
    AGENTS.md, .gitignore, .git/, or .github/ from the temp scaffold — this repo's own versions of
    those files must remain byte-for-byte unchanged. After copying, delete the temporary sibling
    directory entirely, then run `pnpm install` in the repo root to regenerate pnpm-lock.yaml
    against the merged package.json.
  </action>
  <acceptance_criteria>
    package.json exists at the repo root with next/react/react-dom dependencies; app/ contains a
    working default Next.js page; README.md, AGENTS.md, .gitignore, .github/, .claude/, docs/,
    tests/ are unchanged from before this task (git reports zero diff on those paths); the
    temporary sibling scaffold directory no longer exists.
  </acceptance_criteria>
  <verify>
    <automated>git diff --quiet README.md AGENTS.md .gitignore && echo "preserved" && pnpm install 2>&1 | tail -5</automated>
  </verify>
  <done>package.json/app/ exist from the merged scaffold; existing repo files are provably untouched; pnpm install exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy check for server-only and vitest</name>
  <action>Human reviews the SUS-flagged package audit findings below (server-only, vitest) and confirms whether to proceed with installing them in Task 3.</action>
  <what-built>
    Package legitimacy audit for two packages this phase needs that the automated legitimacy
    checker flagged: `server-only` (verdict SUS — reason: no `repository` field in its package.json
    metadata) and `vitest` (verdict SUS — reason: this release is very recent / "too-new" by the
    checker's heuristic).
  </what-built>
  <how-to-verify>
    `server-only`: check https://www.npmjs.com/package/server-only — this is Vercel's own official
    package, explicitly named in Next.js's official `data-security` guide as the recommended
    mechanism for blocking server-only modules from client bundles, at ~9.6M weekly downloads per
    this phase's research. The missing-repository flag is a metadata gap, not a legitimacy signal.

    `vitest`: check https://www.npmjs.com/package/vitest — official `vitest-dev/vitest` GitHub
    repo, ~68.9M weekly downloads, actively maintained; flagged only because this specific release
    is recent, not because of any suspicious signal (no unexpected postinstall script, verified
    repo link).

    Confirm both packages look legitimate on npmjs.com (correct maintainers/org, no
    typosquat-lookalike name, no unexpected postinstall script) before approving the install in
    the next task.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installing server-only and vitest, or describe concerns to block.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install phase dependencies, wire pnpm scripts, add Vitest</name>
  <files>package.json, vitest.config.ts, tests/unit/smoke.test.ts</files>
  <read_first>.planning/phases/01-data-foundation-security/01-RESEARCH.md (Standard Stack, Installation block)</read_first>
  <action>
    Run `pnpm add drizzle-orm@0.45.2 @neondatabase/serverless@1.1.0 zod@4.4.3 server-only@0.0.1`
    for runtime dependencies, then
    `pnpm add -D drizzle-kit@0.31.10 dotenv@17.4.2 vitest@4.1.9 vite-tsconfig-paths@6.1.1` for dev
    dependencies — exact versions per this phase's research (npm-registry-verified this session).
    Add pnpm scripts to package.json: `"dev": "next dev"`, `"build": "next build"`,
    `"lint": "next lint"`, `"typecheck": "tsc --noEmit"`, `"test": "vitest"` — matching
    `.github/workflows/ci.yml`'s existing `lint -> typecheck -> test -> build` sequence exactly
    (CI already runs `pnpm test -- --run`, which resolves to `vitest --run`). Create
    `vitest.config.ts` per the `<interfaces>` block in this plan's context (tsconfigPaths plugin,
    node test environment — no env-var handling yet, that's Plan 01-02). Create
    `tests/unit/smoke.test.ts` with one trivial passing assertion, matching the existing
    `tests/unit/`, `tests/integration/`, `tests/e2e/` convention already scaffolded in this repo
    (see `tests/README.md`) — this keeps `pnpm test -- --run` green from this wave forward instead
    of failing with "no test files found" until Plan 01-02/01-05 add real tests.
  </action>
  <acceptance_criteria>
    package.json lists all eight packages at the pinned versions above; all five scripts exist and
    run; `tests/unit/smoke.test.ts` exists and passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm lint && pnpm run typecheck && pnpm test -- --run && pnpm build</automated>
  </verify>
  <done>All four CI-equivalent commands (lint, typecheck, test, build) exit 0 on the bare scaffold; Phase 1's core+supporting dependencies are installed at pinned versions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev machine -> npm/pnpm registry | Every `pnpm add` pulls and executes arbitrary third-party code (install scripts) from the public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `pnpm add` package installs (drizzle-orm, @neondatabase/serverless, zod, server-only, drizzle-kit, dotenv, vitest, vite-tsconfig-paths) | high | mitigate | Package Legitimacy Audit run this session (`gsd-tools query package-legitimacy check`); `server-only` and `vitest` (both `[SUS]`) gated behind a blocking `checkpoint:human-verify` (Task 2) before install; the remaining six packages are `[OK]`/pre-approved via research's own audit table |
| T-01-01 | Tampering | Bootstrap merge accidentally overwriting existing repo files (README.md, AGENTS.md, .gitignore, .github/) with the generic `create-next-app` template output | medium | mitigate | Task 1 scaffolds into a temp sibling directory and copies an explicit allowlist of framework files only, never a blind directory merge; verified via `git diff --quiet` on the protected paths |
</threat_model>

<verification>
`pnpm lint && pnpm run typecheck && pnpm test -- --run && pnpm build` all exit 0. Existing
scaffold files (README.md, AGENTS.md, .gitignore, .github/, .claude/, docs/, tests/) unchanged.
</verification>

<success_criteria>
Next.js app boots locally; CI-equivalent script surface (`lint`/`typecheck`/`test`/`build`) is
green; `server-only` and `vitest` installs were human-approved per the package-legitimacy gate.
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-security/01-01-SUMMARY.md` when done
</output>
