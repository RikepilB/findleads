---
phase: 01-data-foundation-security
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - tests/integration/db/businesses.test.ts
  - tests/integration/db/leads.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "A businesses row is created on first sighting of a place_id and upserted (not replaced) on every later sighting, without resetting existing notes/contacted state — proven against a real Postgres database, not mocked"
    - "A leads row is written per job scrape event with unique(job_id, place_id) — proven idempotent under a simulated worker retry"
    - "Place content (name/address/phone/rating/website) persists durably and is readable after upsert"
  artifacts:
    - path: "tests/integration/db/businesses.test.ts"
      provides: "DATA-01 + DATA-03 proof against the real Neon test database"
    - path: "tests/integration/db/leads.test.ts"
      provides: "DATA-02 proof against the real Neon test database"
  key_links:
    - from: "tests/integration/db/businesses.test.ts"
      to: "lib/db/businesses.ts upsertBusiness"
      via: "direct import and call against the real test database (vitest.config.ts test.env DATABASE_URL)"
      pattern: "upsertBusiness\\("
    - from: "tests/integration/db/leads.test.ts"
      to: "lib/db/leads.ts insertLeadSnapshot"
      via: "direct import and call, paired with lib/db/jobs.ts createJob for a real FK-satisfying job row"
      pattern: "insertLeadSnapshot\\("
---

<objective>
Prove DATA-01, DATA-02, and DATA-03 concretely, against a real Neon Postgres database (the
isolated test target resolved in Plan 01-02), not just by code inspection. This is the phase's
final Walking Skeleton proof point — a real, automated DB read/write round-trip exercised through
the actual Data Access Layer, not a throwaway smoke script.

Purpose: DATA-01/02/03 are this phase's core success criteria (ROADMAP.md Success Criteria 1-3);
these tests are the durable, re-runnable evidence that the identity/sighting split actually
behaves as designed — not a one-time manual check that bit-rots.
Output: `tests/integration/db/businesses.test.ts`, `tests/integration/db/leads.test.ts` — both
passing, both exercising real inserts/updates/selects against Postgres.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-security/01-RESEARCH.md

<interfaces>
<!-- Data Access Layer functions from Plan 01-04, called directly by these tests -->

```typescript
// lib/db/jobs.ts (already exists from Plan 01-04)
export function createJob(params: { category: string; location: string }): Promise<{ id: string }>
export function getJob(id: string): Promise<Job | undefined>

// lib/db/businesses.ts (already exists from Plan 01-04)
export function upsertBusiness(place: {
  placeId: string
  businessName: string
  phone: string | null
  address: string | null
  website: string | null
  rating: number | null
  reviewCount: number | null
}): Promise<void>

// lib/db/leads.ts (already exists from Plan 01-04)
export function insertLeadSnapshot(jobId: string, place: {
  placeId: string
  businessName: string
  phone: string | null
  address: string | null
  website: string | null
  rating: number | null
  reviewCount: number | null
}): Promise<void>

// lib/db/client.ts (already exists from Plan 01-03) — for direct row assertions/cleanup in tests
export const db: NeonHttpDatabase<typeof schema>
```

`vitest.config.ts` (Plan 01-02) already injects a real, isolated `DATABASE_URL` (the Neon test
target) and a dummy `PLACES_API_KEY` into every Vitest run — these tests need no additional setup
to reach a real database.
</interfaces>
</context>

## Artifacts This Phase Produces (this plan's share)

- `tests/integration/db/businesses.test.ts` — DATA-01, DATA-03 proof
- `tests/integration/db/leads.test.ts` — DATA-02 proof

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: businesses.test.ts — upsert preserves CRM fields, persists content (DATA-01, DATA-03)</name>
  <files>tests/integration/db/businesses.test.ts</files>
  <read_first>.planning/phases/01-data-foundation-security/01-RESEARCH.md (Phase Requirements table DATA-01/DATA-03 rows, Pitfall 1)</read_first>
  <behavior>
    - Test 1 (DATA-03): call `upsertBusiness` once with a full place payload (name, phone,
      address, website, rating, reviewCount); select the row back by `placeId` directly via `db`;
      assert every content field matches what was inserted.
    - Test 2 (DATA-01): call `upsertBusiness` for a `placeId`, then directly update that row's
      `notes`/`contacted` columns via `db.update(businesses)` (simulating what a future CRM Server
      Action will do), then call `upsertBusiness` again for the *same* `placeId` with a
      *different* `businessName`/`phone` (simulating a re-scrape sighting). Assert: the content
      fields (`businessName`/`phone`) reflect the new values (refreshed), `lastSeenAt` advanced,
      but `notes`/`contacted` are unchanged from the value set by the direct update — proving the
      upsert preserved CRM state rather than resetting it.
    - Each test cleans up the rows it inserts (delete by `placeId`) so re-runs stay idempotent.
  </behavior>
  <action>
    Implement the two test cases per the behavior block above, using vitest's `describe`/`it`/
    `expect` and `beforeEach`/`afterEach` (or per-test cleanup) so the test file can be re-run
    against the shared test database without accumulating rows. Import `upsertBusiness` from
    `@/lib/db/businesses`, and `db`/`businesses` from `@/lib/db/client` and `@/lib/db/schema`
    directly for the assertion queries and cleanup deletes.
  </action>
  <acceptance_criteria>
    Both tests pass against the real Neon test database; re-running the file twice in a row
    produces the same result (no leftover-row interference); the second test specifically asserts
    that a value written outside `upsertBusiness` survives a subsequent `upsertBusiness` call for
    the same place.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/integration/db/businesses.test.ts</automated>
  </verify>
  <done>DATA-01 and DATA-03 are proven against a real Postgres database: content persists and is readable, and CRM fields survive a second sighting's upsert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: leads.test.ts — idempotent per-job snapshot insert, businesses join (DATA-02)</name>
  <files>tests/integration/db/leads.test.ts</files>
  <read_first>.planning/phases/01-data-foundation-security/01-RESEARCH.md (Phase Requirements table DATA-02 row, Code Example 2)</read_first>
  <behavior>
    - Test 1 (DATA-02 core): create a real job via `createJob`; call `insertLeadSnapshot(jobId,
      place)` twice with the identical `placeId` (simulating a worker retry/resume re-processing
      the same page). Assert: no error is thrown on the second call, and exactly one row exists in
      `leads` for that `(jobId, placeId)` pair.
    - Test 2 (per-pair scope, not global dedup): using the same job, call `insertLeadSnapshot`
      with a second, different `placeId`. Assert both leads rows now coexist under the same job —
      dedup is scoped to the exact pair, not a global collapse.
    - Test 3 (join at query time, per DATA-02's stated purpose): call `upsertBusiness` for one of
      the leads' `placeId` values, then run a query joining `leads` to `businesses` on `placeId`
      for that job. Assert the join returns a row combining the per-job snapshot with the
      businesses CRM row, demonstrating leads and businesses can be joined for CRM display/export
      as DATA-02 requires — with no foreign key between them, only a shared `placeId` value.
    - Clean up all rows created (leads, businesses, jobs) at the end of each test.
  </behavior>
  <action>
    Implement the three test cases per the behavior block above. Import `createJob` from
    `@/lib/db/jobs`, `insertLeadSnapshot` from `@/lib/db/leads`, `upsertBusiness` from
    `@/lib/db/businesses`, and `db`/`leads`/`businesses` from `@/lib/db/client`/`@/lib/db/schema`
    for the join query and cleanup.
  </action>
  <acceptance_criteria>
    All three tests pass against the real Neon test database; the retry test proves zero rows
    duplicate and zero errors throw; the join test proves a `leads`-to-`businesses` query by
    `placeId` actually returns a combined row.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/integration/db/leads.test.ts</automated>
    <automated>pnpm test -- --run</automated>
  </verify>
  <done>DATA-02 is proven against a real Postgres database: per-(job,place) dedup is idempotent, scoped correctly (not global), and leads/businesses are joinable at query time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test database <-> shared Neon test project/branch | Test data written here must never leak into or be confused with the dev/production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | Test runs leaving residual rows that corrupt later test runs or accumulate indefinitely in the test database | low | mitigate | Every test cleans up the rows it creates (explicit delete-by-key after each test), so the suite is idempotent and safely re-runnable |
| T-05-02 | Information Disclosure | `.env.test`'s `TEST_DATABASE_URL` being accidentally committed or logged during test runs | low | accept | Already mitigated structurally in Plan 01-02 (gitignored, verified via `git check-ignore`); no test in this plan logs the connection string, so residual risk here is a duplicate of T-02-03, not a new exposure surface |
</threat_model>

<verification>
`pnpm vitest run tests/integration/db/businesses.test.ts` and
`pnpm vitest run tests/integration/db/leads.test.ts` both pass. Full suite
(`pnpm test -- --run`) is green, matching `.github/workflows/ci.yml`.
</verification>

<success_criteria>
DATA-01, DATA-02, and DATA-03 — this phase's core data-model success criteria — are proven against
a real Neon Postgres database via automated, re-runnable tests, completing this phase's Walking
Skeleton "real DB read/write round-trip" requirement.
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-security/01-05-SUMMARY.md` when done
</output>
