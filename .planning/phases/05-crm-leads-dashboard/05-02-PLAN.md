---
phase: 05-crm-leads-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - components/GoogleAttribution.tsx
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "Every page in the app (Leads, Job History) renders a persistent two-tab nav plus a Google Maps attribution line placed directly adjacent to the content region on every page — not buried in a distant footer"
    - "Attribution text reads exactly 'Google Maps' (never 'Powered by Google'), at least 12px, never hidden or recolored"
  artifacts:
    - components/GoogleAttribution.tsx (new)
    - app/layout.tsx (nav tabs + attribution wired in)
  key_links:
    - "app/layout.tsx wraps {children} -> both Plan 05-03's /leads and Plan 05-04's /jobs pages inherit the nav + attribution automatically, no per-page duplication needed"
---

<objective>
Replace the untouched create-next-app `app/layout.tsx` with the shared app shell this phase needs: a two-tab nav (Leads / Job History) and a Google Maps attribution line placed immediately below the nav, above the content — satisfying SEC-03 for every page that will show Places-sourced content, without each page having to remember to render its own attribution.

Purpose: SEC-03 requires attribution "on any UI displaying Places content, including the map-less leads table" — putting it once in the shared layout, directly adjacent to where content renders, satisfies this for both pages this phase adds and any future one.
Output: `components/GoogleAttribution.tsx` and a rewritten `app/layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-crm-leads-dashboard/05-UI-SPEC.md
@.planning/phases/05-crm-leads-dashboard/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create components/GoogleAttribution.tsx</name>
  <files>components/GoogleAttribution.tsx</files>
  <read_first>.planning/phases/05-crm-leads-dashboard/05-UI-SPEC.md (Attribution Requirement section — exact copy "Google Maps", 12px, gray-500, un-styled, never behind a collapsed menu; Color table's Typography role "Label/meta" 12px/400)</read_first>
  <action>
    Create `components/GoogleAttribution.tsx` as a plain server-renderable component (no `"use client"` needed — static text) exporting a default function that renders a `&lt;p&gt;` with the literal text "Google Maps" (exact capitalization, per SEC-03 and research Pitfall 2 — this string is NOT "Powered by Google"), styled `text-xs text-[#5E5E5E]` (12px, matches the UI-SPEC gray value) with no link, no icon requirement for v1. Do not add a `href`/`Link` wrapper — v1 plain text satisfies the requirement per UI-SPEC.
  </action>
  <verify>
    <automated>grep -c "Google Maps" components/GoogleAttribution.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exports a component rendering the exact text "Google Maps"
    - No occurrence of the string "Powered by Google" anywhere in the file
    - Text styling is at least 12px (`text-xs` in Tailwind = 12px), not smaller
  </acceptance_criteria>
  <done>components/GoogleAttribution.tsx exists, renders "Google Maps" only, and is ready to import into layout.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Wire two-tab nav + attribution into app/layout.tsx</name>
  <files>app/layout.tsx</files>
  <read_first>app/layout.tsx (current create-next-app scaffold — keep the Geist font setup and `html`/`body` structure, only replace the metadata + add nav/attribution inside body), components/GoogleAttribution.tsx (just created)</read_first>
  <action>
    Edit `app/layout.tsx`: update `metadata` to a real title ("findleads" or similar, not "Create Next App"/"Generated by create next app"). Inside `&lt;body&gt;`, before `{children}`, add a top nav bar (plain `&lt;nav&gt;` using Tailwind utilities per UI-SPEC's spacing/color tokens — `bg-[#F3F4F6]` secondary background, `lg` 24px horizontal padding) containing two `next/link` `&lt;Link&gt;` elements: "Leads" (`href="/leads"`) and "Job History" (`href="/jobs"`). Active-tab styling is a nice-to-have, not required — do not add client-side `usePathname` complexity for this if it requires a `"use client"` boundary on the whole layout; a plain static two-link nav satisfies the UI-SPEC's "no sidebar, two views" requirement without needing active-state detection. Immediately below the nav (still above `{children}`), render `&lt;GoogleAttribution /&gt;` in a slim bar (`bg-[#F3F4F6]` or transparent, `sm` 8px vertical padding) so it is visually adjacent to whatever page content renders next — this placement is what satisfies SEC-03's "within the same visual container" requirement for both `/leads` and `/jobs` per research Pitfall 2's corrected guidance, since both pages render their Places-content table directly below this point with no intervening content.
  </action>
  <verify>
    <automated>grep -c "GoogleAttribution" app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` no longer contains "Create Next App" or "Generated by create next app"
    - Nav contains links to `/leads` and `/jobs`
    - `&lt;GoogleAttribution /&gt;` is rendered above `{children}`, not only in a page-bottom footer
  </acceptance_criteria>
  <done>Visiting any page renders the two-tab nav and "Google Maps" attribution directly above the page content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan adds only static server-rendered layout markup — no user input, no data mutation, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-04 | Information Disclosure | N/A | low | accept | No sensitive data rendered in layout; nav links and attribution text are static, non-parameterized |
</threat_model>

<verification>
`pnpm typecheck` passes. `pnpm build` succeeds (confirms layout compiles as a valid Server Component tree). Visually confirm (manual, end-of-phase) that the nav + "Google Maps" text render above content on both `/leads` and `/jobs` once Plans 05-03/05-04 exist.
</verification>

<success_criteria>
Shared app shell (nav + attribution) exists in `app/layout.tsx`, ready for both page plans to inherit without any per-page attribution duplication.
</success_criteria>

<output>
Create `.planning/phases/05-crm-leads-dashboard/05-02-SUMMARY.md` when done
</output>
