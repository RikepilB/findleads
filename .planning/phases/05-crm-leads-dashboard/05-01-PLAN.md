---
phase: 05-crm-leads-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema.ts
  - lib/db/businesses.ts
  - lib/db/jobs.ts
  - lib/jobs/checkpoint.ts
  - lib/jobs/runScrapeJob.ts
  - drizzle/*.sql (new, auto-generated)
  - tests/integration/db/businesses.test.ts
  - tests/integration/db/jobs.test.ts
  - tests/unit/lib/jobs/runScrapeJob.test.ts
  - tests/unit/lib/jobs/checkpoint.test.ts
autonomous: true
requirements: [CRM-01, CRM-02, CRM-03, CRM-04, CRM-05, SCRAPE-07]

must_haves:
  truths:
    - "listBusinesses() returns every business row with enough data to derive tier (website null/not-null), contacted, and notes at query time"
    - "updateBusinessNotes() and setBusinessContacted() persist their field AND bump updated_at on every call, never leaving updated_at stale (CRM-04)"
    - "listJobs() returns every job ordered newest-first with resultCapHit available per row"
    - "A job that hits the 3-page/60-result Text Search cap has resultCapHit=true even if closed-business filtering drops leadsFound below 60 (SCRAPE-07 false-negative fix, Pitfall 1 Option B)"
  artifacts:
    - lib/db/businesses.ts (listBusinesses, updateBusinessNotes, setBusinessContacted added)
    - lib/db/jobs.ts (listJobs added; updateJobProgress accepts resultCapHit)
    - lib/db/schema.ts (jobs.resultCapHit boolean column)
    - lib/jobs/checkpoint.ts (JobCursor.capHit field)
    - lib/jobs/runScrapeJob.ts (computes and persists capHit)
    - drizzle/000X_*.sql (additive migration for resultCapHit)
  key_links:
    - "runScrapeJob.ts's defaultFetchOnePage cap-hit computation -> cursor.capHit -> final updateJobProgress('done') write -> jobs.resultCapHit column -> read by Plan 05-04's Job History page"
    - "updateBusinessNotes/setBusinessContacted -> businesses.updatedAt -> read by Plan 05-03's Leads page relative-time display"
---

<objective>
Add the data-access layer this phase's UI needs (listBusinesses, updateBusinessNotes, setBusinessContacted, listJobs), and fix the SCRAPE-07 false-negative by adding an additive `resultCapHit` column computed at the exact point the worker knows the 60-result cap was genuinely hit — before closed-business filtering can silently erase that signal.

Purpose: Every later plan in this phase reads or writes through these DAL functions; this plan has no direct UI but is the necessary plumbing goal-backward from CRM-01..05 and SCRAPE-07.
Output: Extended `lib/db/businesses.ts` / `lib/db/jobs.ts`, extended schema + migration, worker change in `lib/jobs/runScrapeJob.ts`/`lib/jobs/checkpoint.ts`, and integration/unit tests proving all of the above against the real test DB.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-crm-leads-dashboard/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add listBusinesses/updateBusinessNotes/setBusinessContacted (businesses.ts) and listJobs (jobs.ts)</name>
  <files>lib/db/businesses.ts, lib/db/jobs.ts, tests/integration/db/businesses.test.ts, tests/integration/db/jobs.test.ts</files>
  <read_first>lib/db/businesses.ts (existing upsertBusiness — mirror its explicit updatedAt-set discipline exactly), lib/db/jobs.ts (existing createJob/getJob/updateJobProgress pattern), lib/db/schema.ts (businesses/jobs column names), tests/integration/db/businesses.test.ts (existing test shape: PLACE_ID const, afterEach cleanup, real-DB assertions — extend this exact pattern, do not introduce a new test style)</read_first>
  <behavior>
    - listBusinesses(): returns all rows from `businesses`, ordered by `updatedAt` descending; a row with `website: null` is the tier-1 signal (no separate column) and a row with `website` set is not tier-1.
    - updateBusinessNotes(id, notes): sets `notes` and `updatedAt: new Date()` in the same `.set({...})` call (Pitfall 3 — Drizzle does not auto-bump `.defaultNow()` columns on UPDATE); a second call with different notes text changes `updatedAt` again on each call.
    - setBusinessContacted(id, contacted): same discipline — sets `contacted` and `updatedAt: new Date()` together, never touching `businessName`/`website`/`notes`/other columns.
    - listJobs(): returns all rows from `jobs`, ordered by `createdAt` descending.
  </behavior>
  <action>
    In `lib/db/businesses.ts`, add three exported functions following the file's existing `upsertBusiness` idiom exactly (import `eq`/`desc` from `drizzle-orm`, use `db.update(businesses).set({...}).where(eq(businesses.id, id))` for the two mutations, `db.select().from(businesses).orderBy(desc(businesses.updatedAt))` for the list). `updateBusinessNotes(id: number, notes: string): Promise&lt;void&gt;` sets only `notes` and `updatedAt`. `setBusinessContacted(id: number, contacted: boolean): Promise&lt;void&gt;` sets only `contacted` and `updatedAt`. `listBusinesses()` returns `Awaited&lt;ReturnType&lt;typeof db.select&gt;&gt;`-shaped rows (let TypeScript infer). In `lib/db/jobs.ts`, add `listJobs()` returning `db.select().from(jobs).orderBy(desc(jobs.createdAt))`. Extend `tests/integration/db/businesses.test.ts` with a new `describe` block covering all three new functions against the real test DB (same `PLACE_ID`-style unique fixture + `afterEach` cleanup pattern already in the file — do not touch the two existing `it` blocks). Create `tests/integration/db/jobs.test.ts` (new file, does not exist yet) mirroring the same structural pattern (create a job via `createJob`, assert `listJobs()` includes it ordered correctly, clean up via `db.delete(jobs).where(...)` in `afterEach`).
  </action>
  <verify>
    <automated>pnpm test -- tests/integration/db/businesses.test.ts tests/integration/db/jobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `listBusinesses`, `updateBusinessNotes`, `setBusinessContacted` are exported from `lib/db/businesses.ts` with the signatures above
    - `listJobs` is exported from `lib/db/jobs.ts`
    - Both mutation functions include `updatedAt: new Date()` in their `.set()` call (grep: `grep -c "updatedAt: new Date()" lib/db/businesses.ts` returns at least 3, covering `upsertBusiness` + the two new functions)
    - New/extended tests pass against the real test database, proving persistence and the `updatedAt` bump on each mutation call
  </acceptance_criteria>
  <done>listBusinesses/updateBusinessNotes/setBusinessContacted/listJobs exist, are used successfully by their new integration tests, and every mutation visibly bumps updatedAt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SCRAPE-07 Option B — persist a genuine resultCapHit signal, computed before closed-business filtering can hide it</name>
  <files>lib/db/schema.ts, lib/db/jobs.ts, lib/jobs/checkpoint.ts, lib/jobs/runScrapeJob.ts, drizzle/*.sql (new, generated), tests/unit/lib/jobs/runScrapeJob.test.ts, tests/unit/lib/jobs/checkpoint.test.ts</files>
  <read_first>lib/jobs/runScrapeJob.ts (defaultFetchOnePage — `pagesFetched`/`hasMore`/`response.nextPageToken` logic; the cap-hit condition is `pagesFetched >= MAX_PAGES && Boolean(response.nextPageToken)`, computed BEFORE `mapped` is filtered by mapPlaceToLead), lib/jobs/checkpoint.ts (JobCursor interface, MAX_PAGES), lib/db/jobs.ts (updateJobProgress signature — extend, do not replace), tests/unit/lib/jobs/runScrapeJob.test.ts (existing mock-based test pattern — vi.mock at module boundary, `makeJob`/`lead` helpers), .planning/phases/05-crm-leads-dashboard/05-RESEARCH.md Pitfall 1 (Option B rationale, exact condition)</read_first>
  <behavior>
    - Given a fetch response where `pagesFetched` reaches `MAX_PAGES` (3) AND that final page's `response.nextPageToken` is truthy, the resulting cursor carries `capHit: true` — regardless of how many of that page's places were later filtered out as closed businesses.
    - Given a job whose pagination ends naturally (no more pages, or fewer than MAX_PAGES fetched), the cursor carries `capHit: false`.
    - Once any page during a job's run sets `capHit: true`, that value survives subsequent checkpoints and is written to `jobs.resultCapHit` on the final `status: 'done'` write (not lost when `cursor` itself is nulled out).
  </behavior>
  <action>
    In `lib/db/schema.ts`, add `resultCapHit: boolean('result_cap_hit').notNull().default(false)` to the `jobs` table definition (additive column, same category as Phase 3's `leadsFound`/`cursor`/`errorReason`). Run `pnpm drizzle-kit generate` to produce the new migration file under `drizzle/` (do not hand-write the SQL). In `lib/jobs/checkpoint.ts`, add `capHit: boolean` to the `JobCursor` interface and set `capHit: false` in `initialCursor()`. In `lib/jobs/runScrapeJob.ts`'s `defaultFetchOnePage`, compute `const capHit = pagesFetched >= MAX_PAGES && Boolean(response.nextPageToken)` immediately after `pagesFetched` is derived (before the `mapped` filter runs, using the raw `response.places.length`/`response.nextPageToken`, not the post-filter count), and carry it forward: `nextCursor.capHit = cursor.capHit || capHit` (once true, stays true across checkpoints — a job's cap-hit state is monotonic within a run). In the main `runScrapeJob` loop, thread `cursor.capHit` through to the final `updateJobProgress(jobId, { status: 'done', leadsFound, cursor: null, resultCapHit: cursor.capHit })` call. Extend `updateJobProgress` in `lib/db/jobs.ts` to accept an optional `resultCapHit?: boolean` param and include it in `.set({...})` only when provided (default the column value via schema default when omitted, so the two existing non-final calls to `updateJobProgress` — which do not pass `resultCapHit` — are unaffected). Extend `tests/unit/lib/jobs/runScrapeJob.test.ts` (or `checkpoint.test.ts`, whichever already has the closest fixture) with a case using the existing `text-search-with-closed-business.json` fixture at `pagesFetched === MAX_PAGES` with a truthy `nextPageToken`, asserting the final `updateJobProgress` call receives `resultCapHit: true` even though the closed business in that fixture reduces `leadsFound` below the raw page count.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/lib/jobs/runScrapeJob.test.ts tests/unit/lib/jobs/checkpoint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `jobs.resultCapHit` column exists in `lib/db/schema.ts` and a matching migration file exists under `drizzle/`
    - `JobCursor` has a `capHit: boolean` field, initialized `false`
    - The cap-hit condition is computed from raw page/token data, not from post-filter `leadsFound`, verifiable by reading the diff in `runScrapeJob.ts`
    - A test proves a closed-business-containing final page at `pagesFetched === MAX_PAGES` with a truthy `nextPageToken` still yields `resultCapHit: true` on the final DB write
  </acceptance_criteria>
  <done>A job that genuinely exhausts the 3-page/60-result cap is flagged resultCapHit=true on completion, independent of how many results were filtered as closed businesses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Places API response -> runScrapeJob -> Postgres | Untrusted upstream data (place names/addresses) already validated/mapped by Phase 2; this plan only adds a boolean derived from pagination metadata, no new untrusted-string surface |
| Future Server Action (Plan 05-03) -> businesses.ts mutation functions | This plan's `updateBusinessNotes`/`setBusinessContacted` take a raw `id`/`notes`/`contacted` — no validation happens at this DAL layer itself (by design; validation is the caller's job, per Pattern 2 in research) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-01 | Tampering | `updateBusinessNotes`/`setBusinessContacted` (no input validation at DAL layer) | medium | mitigate | DAL functions are internal-only, not exposed directly to HTTP; validation is enforced at the Server Action boundary added in Plan 05-03 (zod: businessId coerced positive int, notes trimmed+capped 2000 chars) — documented here so Plan 05-03 does not skip it |
| T-05-02 | Information Disclosure | `resultCapHit` derived from raw Places response metadata | low | accept | Boolean only, no new PII/content surface exposed beyond what Phase 2/3 already persist |
| T-05-03 | Denial of Service | Additive migration on `jobs` table | low | accept | `NOT NULL DEFAULT false` — safe against existing rows, no backfill script required, no lock beyond a standard `ALTER TABLE ADD COLUMN` with a constant default (fast on Postgres) |
</threat_model>

<verification>
Run `pnpm test -- tests/integration/db/businesses.test.ts tests/integration/db/jobs.test.ts tests/unit/lib/jobs/runScrapeJob.test.ts tests/unit/lib/jobs/checkpoint.test.ts` — all green. Run `pnpm typecheck` — no new errors. Confirm `drizzle/` contains exactly one new migration file beyond `0001_tan_stepford_cuckoos.sql`.
</verification>

<success_criteria>
All four new/extended DAL functions exist and are proven against the real test database; `jobs.resultCapHit` is additive, migrated, and set correctly by the worker at job completion, closing the SCRAPE-07 false-negative gap identified in research Pitfall 1.
</success_criteria>

<output>
Create `.planning/phases/05-crm-leads-dashboard/05-01-SUMMARY.md` when done
</output>
