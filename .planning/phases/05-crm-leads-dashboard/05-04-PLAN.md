---
phase: 05-crm-leads-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - app/jobs/page.tsx
  - app/jobs/JobForm.tsx
  - app/jobs/JobStatusPoller.tsx
  - tests/unit/app/jobs/JobStatusPoller.test.ts
autonomous: false
requirements: [CRM-05, SCRAPE-07]
user_setup: []

must_haves:
  truths:
    - "Richard can view job history: status, category/location, leads found, created_at (CRM-05)"
    - "A job that hit the 60-result Text Search cap shows '60+ results found, showing first 60 — refine your search.' (SCRAPE-07)"
    - "A non-terminal job (pending/running/partial) is actively polled client-side via GET /api/jobs/[id] so Phase 4's JOB-04 continuation and JOB-05 stale watchdog actually trigger while Richard is watching the page, instead of sitting dormant"
    - "Richard can submit a new category+location scrape from this page (incidental scope per UI-SPEC Assumption #2 — no tracked requirement, but necessary for the product to be usable end-to-end)"
  artifacts:
    - app/jobs/page.tsx (new)
    - app/jobs/JobForm.tsx (new, client)
    - app/jobs/JobStatusPoller.tsx (new, client)
  key_links:
    - "app/jobs/page.tsx -> listJobs() (Plan 05-01) -> table render, one JobStatusPoller per non-terminal row"
    - "JobStatusPoller -> GET /api/jobs/[id] (existing, unchanged) -> claimPartialJob/flagStaleJob/after(runScrapeJob) -> keeps JOB-04/JOB-05 alive"
    - "JobForm -> POST /api/jobs (existing, unchanged) -> router.refresh() -> new pending row appears in listJobs()"
---

<objective>
Build the Job History page: a Server Component listing all jobs (status, category/location, leads found, created_at, SCRAPE-07 cap notice), a client-side job-creation form posting to the existing `POST /api/jobs`, and a client-side poller that keeps hitting `GET /api/jobs/[id]` for any non-terminal job so Phase 4's continuation/watchdog mechanisms don't go dormant once nothing else is watching them.

Purpose: CRM-05 (job history view) and SCRAPE-07 (cap-hit messaging) are this plan's tracked requirements; the poller is necessary plumbing so JOB-04/JOB-05 (already built in Phase 4) actually fire for jobs Richard is currently viewing — without it, a `partial` job sits forever with nothing left to advance it once this UI exists as the primary way to watch a job.
Output: `app/jobs/page.tsx`, `app/jobs/JobForm.tsx`, `app/jobs/JobStatusPoller.tsx`, `swr` installed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-crm-leads-dashboard/05-UI-SPEC.md
@.planning/phases/05-crm-leads-dashboard/05-RESEARCH.md
@.planning/phases/05-crm-leads-dashboard/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy check for swr</name>
  <action>Human reviews the SUS-flagged package audit finding below (swr) and confirms whether to proceed with installing it in Task 2, or use the dependency-free fallback.</action>
  <what-built>Nothing yet — this checkpoint gates installing the `swr` package, flagged `[SUS]` by the automated package-legitimacy scan in 05-RESEARCH.md's Package Legitimacy Audit (the flag fired on the recency of the latest published version, not the package's actual history).</what-built>
  <how-to-verify>
    Before `pnpm add swr` runs, verify independently: (1) run `npm view swr` and confirm the repository field points to `github.com/vercel/swr` and weekly downloads are in the millions; (2) confirm `swr` was already recommended in `.planning/research/STACK.md` from Phase 1's own research, specifically for this exact `GET /api/jobs/:id` polling use case. Both were true as of this session's research (2026-07-03) — re-confirm if executing significantly later.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `pnpm add swr@2.4.2`, or "use fallback" to build `JobStatusPoller.tsx` with the dependency-free `fetch`+`setInterval` fallback documented in 05-RESEARCH.md instead (functionally equivalent, no package installed).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install swr (if approved) + app/jobs/JobStatusPoller.tsx</name>
  <files>package.json, pnpm-lock.yaml, app/jobs/JobStatusPoller.tsx, tests/unit/app/jobs/JobStatusPoller.test.ts</files>
  <read_first>.planning/phases/05-crm-leads-dashboard/05-RESEARCH.md Pattern 3 (both the `swr`-based example and the dependency-free fallback, in full), app/api/jobs/[id]/route.ts (response shape this component polls: `{ id, status, category, location, leadsFound, errorReason, createdAt, updatedAt }`)</read_first>
  <behavior>
    - isTerminalStatus('done') and isTerminalStatus('error') both return true; isTerminalStatus('pending')/('running')/('partial') all return false.
    - Given `initialStatus` is already terminal, the component does not issue any poll request.
    - Given `initialStatus` is non-terminal, the component polls `GET /api/jobs/{jobId}` on an interval and stops polling once the response's `status` becomes terminal.
  </behavior>
  <action>
    If the checkpoint above was approved: run `pnpm add swr` (pins to `2.4.2` per research's verified version). Create `app/jobs/JobStatusPoller.tsx` with `'use client'`. Export a pure, independently-testable `isTerminalStatus(status: string): boolean` helper (`status === 'done' || status === 'error'`). Implement the default export `JobStatusPoller({ jobId, initialStatus }: { jobId: string; initialStatus: string })` using `useSWR` with `refreshInterval: (latest) => (latest && isTerminalStatus(latest.status) ? 0 : 1500)` and `null` as the key when `isTerminalStatus(initialStatus)` is already true (per the research code example), rendering a status badge from the polled `status` (fall back to `initialStatus` before the first response) using the UI-SPEC's status badge color table (pending/running gray, partial amber, done-with-leads green, done-zero-leads neutral gray per JOB-06, error red). If the checkpoint was declined ("use fallback"), implement the same public interface (`isTerminalStatus` + `JobStatusPoller`) using the dependency-free `useEffect`+`setInterval`+`fetch` fallback from research instead — do not install `swr` in that case. Create `tests/unit/app/jobs/JobStatusPoller.test.ts` testing only the exported `isTerminalStatus` pure function (per 05-RESEARCH.md's Validation Architecture — async Server Components and client hook components are not renderable under this project's Vitest `react-server` condition; do not add `@testing-library/react`/`jsdom`).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/app/jobs/JobStatusPoller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `isTerminalStatus` is exported and correctly classifies all five job statuses
    - `JobStatusPoller` never polls once `initialStatus` is already `done`/`error`
    - No `@testing-library/react` or `jsdom` dependency was added
  </acceptance_criteria>
  <done>JobStatusPoller.tsx exists (swr-based or fallback-based per the checkpoint's outcome), its terminal-status logic is unit-tested, and it is ready to be rendered per-row in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: app/jobs/JobForm.tsx + app/jobs/page.tsx</name>
  <files>app/jobs/JobForm.tsx, app/jobs/page.tsx</files>
  <read_first>.planning/phases/05-crm-leads-dashboard/05-UI-SPEC.md (Copywriting Contract — "Start Scrape" CTA, empty-state copy, zero-result/error-state copy, SCRAPE-07 exact cap-hit message text, status badge colors), app/api/jobs/route.ts (existing POST body shape `{ category, location }` this form must POST, unchanged), lib/db/jobs.ts (listJobs row shape including `resultCapHit` from Plan 05-01)</read_first>
  <action>
    Create `app/jobs/JobForm.tsx` with `'use client'`: a small form with `category`/`location` text inputs and a "Start Scrape" submit button (accent blue-600 per UI-SPEC), `onSubmit` calls `fetch('/api/jobs', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ category, location }) })`, then on success calls `router.refresh()` (from `next/navigation`'s `useRouter`) so the Server Component re-fetches `listJobs()` and the new `pending` row appears; clear the form fields on success, show a simple inline error if the fetch response is not ok. Create `app/jobs/page.tsx` as an async Server Component: `const rows = await listJobs()`; render `&lt;JobForm /&gt;` above the table. If `rows.length === 0`, render the UI-SPEC empty state ("No jobs yet" / "Start your first scrape above — it'll appear here once it's running."). Otherwise render a table with columns: status badge (rendered via `&lt;JobStatusPoller jobId={r.id} initialStatus={r.status} /&gt;` for every row — the poller itself decides whether to actually poll based on `isTerminalStatus`), category, location, `leadsFound`, `createdAt`, an inline SCRAPE-07 notice ("60+ results found, showing first 60 — refine your search.") rendered when `r.resultCapHit` is true, the JOB-06 zero-result copy ("0 leads found." / "This search came back empty — try a broader category or a larger location. Not every category+location combination has businesses without a website.") when `status === 'done' &amp;&amp; leadsFound === 0`, the error-state copy ("This job failed." / "{error_reason}. Check the category and location, then start a new scrape — failed jobs aren't retried automatically.") when `status === 'error'`, and an "Export CSV" link to `/api/jobs/{r.id}/export` (existing route, unchanged) when `status === 'done'`.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm build</automated>
    <human-check>Visit /jobs, submit a real category+location scrape via the form, and watch the row's status badge advance from pending -&gt; running -&gt; done without a manual page reload (proves the poller is live and JOB-04/JOB-05 still fire). Separately, run a query expected to return 60+ raw results (e.g. "restaurant" in a dense downtown area) and confirm the cap-hit message appears on that row once done.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "60+ results found, showing first 60" app/jobs/page.tsx` is at least 1 (verbatim SCRAPE-07 copy present)
    - `grep -c "No jobs yet" app/jobs/page.tsx` is at least 1
    - Every job row renders a `JobStatusPoller`, and the "Export CSV" link only appears for `done` jobs
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Visiting /jobs shows the creation form and a live-updating job history table, with the SCRAPE-07 cap message firing correctly off the resultCapHit column from Plan 05-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser fetch -> POST /api/jobs | Already-existing, already-validated boundary (Phase 3) — this plan only adds a client caller, no new server-side surface |
| Browser poll -> GET /api/jobs/[id] | Already-existing, already-validated boundary (Phase 4) — this plan only adds a client caller |
| npm registry -> pnpm add swr | Supply-chain trust boundary for a new third-party dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-SC | Tampering | `pnpm add swr` (flagged [SUS] by legitimacy scan) | high | mitigate | Blocking `checkpoint:human-verify` (Task 1) before install; verified against `npm view swr`, download count, and prior STACK.md recommendation; dependency-free fallback available if declined |
| T-05-09 | Denial of Service | Client poller left running indefinitely on a stuck non-terminal job | low | accept | JOB-05's server-side watchdog (Phase 4, unchanged) flips any truly stale job to `error` within `WATCHDOG_MS` (~8.3min) regardless of client poll behavior — the poller cannot loop forever against a job the server has already terminalized |
| T-05-10 | Tampering | JobForm posts arbitrary category/location strings | low | mitigate | Existing `createJobSchema` (Zod, Phase 3, unchanged) validates server-side regardless of what the client sends |
</threat_model>

<verification>
`pnpm test -- tests/unit/app/jobs/JobStatusPoller.test.ts` green. `pnpm typecheck` and `pnpm build` succeed. Manual pass: submit a scrape, observe live status progression without reload; confirm a cap-hitting query shows the SCRAPE-07 message; confirm CSV export link works for a completed job.
</verification>

<success_criteria>
CRM-05 and SCRAPE-07 are fully deliverable end-to-end, and Phase 4's JOB-04/JOB-05 mechanisms remain reachable through this phase's UI instead of going dormant.
</success_criteria>

<output>
Create `.planning/phases/05-crm-leads-dashboard/05-04-SUMMARY.md` when done
</output>
