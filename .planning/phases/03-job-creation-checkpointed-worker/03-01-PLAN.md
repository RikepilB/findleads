---
phase: 03-job-creation-checkpointed-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/jobs/checkpoint.ts
  - lib/jobs/buildTextQuery.ts
  - lib/db/schema.ts
  - lib/db/jobs.ts
  - drizzle/
  - tests/unit/lib/jobs/checkpoint.test.ts
  - tests/unit/lib/jobs/buildTextQuery.test.ts
autonomous: true
requirements: [JOB-02, JOB-03]
must_haves:
  truths:
    - "lib/jobs/checkpoint.ts exports a single JobCursor shape and SAFETY_WINDOW_MS/MAX_PAGES constants that the worker (Plan 03-02) uses as its sole source of truth for checkpoint/resume state"
    - "The jobs table gains leads_found/cursor/error_reason columns via a new additive migration, applied to both the dev and test Neon databases, without editing the first (Phase 1) migration file"
    - "lib/db/jobs.ts exposes updateJobProgress so the worker can persist status/leads_found/cursor after every unit of work"
  artifacts:
    - path: "lib/jobs/checkpoint.ts"
      provides: "JobCursor type, initialCursor(), SAFETY_WINDOW_MS (250_000), MAX_PAGES (3)"
      contains: "export const SAFETY_WINDOW_MS"
    - path: "lib/jobs/buildTextQuery.ts"
      provides: "buildTextQuery(category, location) composing category+location into Google's textQuery format"
      contains: "export function buildTextQuery"
    - path: "lib/db/schema.ts"
      provides: "jobs table extended with leadsFound/cursor/errorReason columns (additive, Phase 1 file)"
      contains: "cursor: jsonb"
    - path: "lib/db/jobs.ts"
      provides: "updateJobProgress(jobId, params) persisting status/leadsFound/cursor/errorReason (Phase 1 file, extended)"
      contains: "export async function updateJobProgress"
  key_links:
    - from: "lib/db/schema.ts jobs.cursor column"
      to: "lib/jobs/checkpoint.ts JobCursor"
      via: "jsonb('cursor').$type<JobCursor>()"
      pattern: "\\$type<JobCursor>"
    - from: "lib/db/jobs.ts updateJobProgress"
      to: "lib/db/schema.ts jobs table"
      via: "db.update(jobs).set({...}).where(eq(jobs.id, jobId))"
      pattern: "updateJobProgress"
---

## Phase Goal

**As a** solo lead-generation user (Richard), **I want to** submit a category + free-text location
scrape request and have it run as a safe, resumable background job, **so that** I can kick off a
Places API scrape without the request blocking on network calls or a long-running job silently
dying past Vercel's execution-time ceiling.

<objective>
Build the checkpoint/resume primitives (`JobCursor`, `SAFETY_WINDOW_MS`, `MAX_PAGES`,
`buildTextQuery`) and extend Phase 1's `jobs` schema/DAL with the three columns and one function
the checkpointed worker (Plan 03-02) needs to persist progress after every unit of work.

Purpose: this is the substrate JOB-02 (checkpointed progress) and JOB-03 (safety-window cutoff)
build on — the cursor shape, the safety-window constant, and the DB column/write-path all have to
exist and typecheck before `runScrapeJob` (Plan 03-02) can be written against them.
Output: `lib/jobs/checkpoint.ts`, `lib/jobs/buildTextQuery.ts`, an additive migration extending
`jobs`, and `lib/db/jobs.ts`'s new `updateJobProgress` function — all typechecking and unit-tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-job-creation-checkpointed-worker/03-RESEARCH.md

<interfaces>
<!-- Phase 1 lib/db/schema.ts and lib/db/jobs.ts are NOT YET on disk (Phase 1 Waves 3-5 are
     blocked on real Neon/Google Cloud secrets) — this plan is written against the LOCKED
     interface from 01-03-PLAN.md/01-04-PLAN.md so it is ready to execute the moment Phase 1
     lands. Do not assume these files exist yet; write them as described if they don't, or
     extend them in place if Phase 1 has since executed. -->

```typescript
// lib/db/schema.ts (Phase 1, Plan 01-03) — BEFORE this plan's Task 2 change:
export const jobStatusEnum: PgEnum<['pending', 'running', 'partial', 'done', 'error']>
export const jobs = pgTable('jobs', {
  id: uuid('id').defaultRandom().primaryKey(),
  status: jobStatusEnum('status').notNull().default('pending'),
  category: text('category').notNull(),
  location: text('location').notNull(),
  createdAt: timestamp('created_at').notNull().defaultNow(),
  updatedAt: timestamp('updated_at').notNull().defaultNow(),
})

// lib/db/jobs.ts (Phase 1, Plan 01-04) — BEFORE this plan's Task 2 change:
export async function createJob(params: { category: string; location: string }): Promise<{ id: string }>
export async function getJob(id: string): Promise<Job | undefined> // Job = typeof jobs.$inferSelect

// lib/db/client.ts (Phase 1, Plan 01-03) — unchanged by this plan
export const db: NeonHttpDatabase<typeof schema>
```

`drizzle.config.ts` (Phase 1, Plan 01-03) already exists — `dialect: 'postgresql'`,
`out: './drizzle'`, credentials from `process.env.DATABASE_URL`. This plan runs
`drizzle-kit generate`/`migrate` against it exactly as Plan 01-03 did.
</interfaces>
</context>

## Artifacts This Phase Produces (this plan's share)

- `lib/jobs/checkpoint.ts` — `JobCursor`, `initialCursor()`, `SAFETY_WINDOW_MS`, `MAX_PAGES`
- `lib/jobs/buildTextQuery.ts` — `buildTextQuery(category, location): string`
- `lib/db/schema.ts` — `jobs` table gains `leadsFound`, `cursor`, `errorReason` columns
- `lib/db/jobs.ts` — `updateJobProgress(jobId, params): Promise<void>`
- `drizzle/000x_*.sql` — new additive migration, applied to dev + test Neon databases

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkpoint.ts + buildTextQuery.ts — cursor shape, safety window, query composition</name>
  <files>lib/jobs/checkpoint.ts, lib/jobs/buildTextQuery.ts, tests/unit/lib/jobs/checkpoint.test.ts, tests/unit/lib/jobs/buildTextQuery.test.ts</files>
  <read_first>.planning/phases/03-job-creation-checkpointed-worker/03-RESEARCH.md (Code Examples 2, 4; Pattern 3; Pitfall 3; Pitfall 4; Open Question 2)</read_first>
  <behavior>
    - `initialCursor()` returns `{ pageToken: null, pagesFetched: 0, done: false }`
    - `SAFETY_WINDOW_MS` is exactly `250_000` and `MAX_PAGES` is exactly `3`, both exported named constants (not inlined magic numbers anywhere else)
    - `buildTextQuery('restaurant', 'Toronto, ON')` returns a string containing both `'restaurant'` and `'Toronto, ON'` (the documented Google format: `"${category} in ${location}"`)
    - `buildTextQuery` never returns just `category` alone — location must always be present in the composed string, even for an unrecognized/default-locale location
  </behavior>
  <action>
    Create `lib/jobs/checkpoint.ts` exporting the `JobCursor` interface (`pageToken: string | null`,
    `pagesFetched: number`, `done: boolean`), `initialCursor()`, `SAFETY_WINDOW_MS = 250_000` (headroom
    under Vercel Hobby's non-configurable 300s ceiling), and `MAX_PAGES = 3` (Text Search's documented
    hard cap, enforced defensively per Open Question 2) — exactly per Code Example 4. Create
    `lib/jobs/buildTextQuery.ts` exporting `buildTextQuery(category: string, location: string): string`
    returning `${category} in ${location}` per Code Example 2 / Pattern 3 — this is the only
    geographic-scoping mechanism available through Phase 2's locked `TextSearchParams` interface
    (no `locationBias`/`locationRestriction` field exists on it); `regionCode` alone is a soft
    ranking bias, not a geographic filter, so `category` must never be sent without `location`
    composed in (Pitfall 3). Write unit tests for both modules per the behavior block above.
  </action>
  <acceptance_criteria>
    `checkpoint.ts` exports `JobCursor`/`initialCursor`/`SAFETY_WINDOW_MS`/`MAX_PAGES` exactly as
    named with the exact values above; `buildTextQuery` always composes both `category` and
    `location` into one string; both unit test files pass.
  </acceptance_criteria>
  <verify>
    <automated>pnpm run typecheck</automated>
    <automated>pnpm vitest run tests/unit/lib/jobs/checkpoint.test.ts</automated>
    <automated>pnpm vitest run tests/unit/lib/jobs/buildTextQuery.test.ts</automated>
  </verify>
  <done>lib/jobs/checkpoint.ts and lib/jobs/buildTextQuery.ts exist, typecheck cleanly, and their unit tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend jobs schema (leads_found/cursor/error_reason) + additive migration + updateJobProgress</name>
  <files>lib/db/schema.ts, drizzle/, lib/db/jobs.ts</files>
  <read_first>.planning/phases/03-job-creation-checkpointed-worker/03-RESEARCH.md (Code Examples 5, 6; Pattern 2; Pitfall 4); .planning/phases/01-data-foundation-security/01-03-PLAN.md (Task 2 — the same generate/read-before-apply/migrate-both-databases discipline this task reuses)</read_first>
  <action>
    Modify the existing `jobs` table definition in `lib/db/schema.ts` (Phase 1, Plan 01-03) to add
    three columns, per Code Example 5: `leadsFound: integer('leads_found').notNull().default(0)`;
    `cursor: jsonb('cursor').$type<JobCursor>()` (import `JobCursor` from `@/lib/jobs/checkpoint`,
    created in Task 1 of this plan — `.$type<T>()` is compile-time typing only, per Drizzle's own
    docs, and needs no additional Zod validation since `cursor` is only ever written by this
    codebase's own code, never parsed from untrusted input); `errorReason: text('error_reason')`
    (nullable). Do NOT touch the generated `0000_*.sql` migration file — run
    `pnpm exec drizzle-kit generate` to produce a NEW migration file. Read the generated SQL before
    applying it: it must contain only `ADD COLUMN` statements, no `DROP`. Run
    `pnpm exec drizzle-kit migrate` against the dev `DATABASE_URL` from `.env`, then apply the same
    migration to the test database by temporarily pointing `DATABASE_URL` at `TEST_DATABASE_URL`
    for a second `drizzle-kit migrate` invocation (both databases must end up on the identical
    schema, matching Plan 01-03's own dev+test discipline). Extend `lib/db/jobs.ts` (already
    exporting `createJob`/`getJob` from Plan 01-04) with `updateJobProgress(jobId: string, params:
    { status: JobStatus; leadsFound: number; cursor: JobCursor | null; errorReason?: string | null
    }): Promise<void>` per Code Example 6 — sets `status`, `leadsFound`, `cursor`,
    `errorReason: params.errorReason ?? null`, and `updatedAt: new Date()`, scoped by
    `eq(jobs.id, jobId)`.
  </action>
  <acceptance_criteria>
    `jobs` table gains `leadsFound`/`cursor`/`errorReason`; the new migration file contains only
    `ADD COLUMN` statements (no `DROP`); `updateJobProgress` is exported from `lib/db/jobs.ts` and
    typechecks against `jobStatusEnum`'s values.
  </acceptance_criteria>
  <verify>
    <automated>pnpm run typecheck</automated>
    <automated>pnpm exec drizzle-kit generate 2>&1 | tail -20</automated>
    <automated>ls drizzle/0001_*.sql >/dev/null 2>&1 && ! grep -qi 'DROP' drizzle/0001_*.sql</automated>
    <automated>DATABASE_URL="$(grep '^DATABASE_URL=' .env | cut -d= -f2-)" pnpm exec drizzle-kit migrate</automated>
  </verify>
  <done>drizzle/ contains the new additive migration applied to both dev and test Neon databases; lib/db/jobs.ts exports updateJobProgress, typechecking cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `lib/jobs/checkpoint.ts`'s `cursor` shape -> `jobs.cursor` jsonb column | This phase's own code is the only writer of this column — never parsed from external/untrusted input |
| Additive migration -> the shared dev/test Neon databases | The migration must never contain a `DROP`, protecting Phase 1's already-live schema and data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | A hand-edited or mis-generated migration accidentally dropping/altering an existing Phase 1 column instead of only adding new ones | high | mitigate | `drizzle-kit generate` produces the migration (not hand-written SQL); Task 2's automated verify greps the generated file for the absence of `DROP` before it is applied |
| T-01-02 | Information Disclosure | `cursor` jsonb column holding a `pageToken` value — a Google-issued pagination token, not itself sensitive but stored durably | low | accept | `pageToken` is not a secret (it is not `PLACES_API_KEY`), has no cross-user meaning in this single-user app, and is cleared to `null` once a job reaches `done` |
</threat_model>

<verification>
`pnpm run typecheck` passes. `pnpm vitest run tests/unit/lib/jobs/checkpoint.test.ts` and
`tests/unit/lib/jobs/buildTextQuery.test.ts` both pass. The new migration applies cleanly to both
dev and test Neon databases with no `DROP` statements.
</verification>

<success_criteria>
The checkpoint/resume primitives and the DB substrate JOB-02/JOB-03 depend on exist and typecheck:
`JobCursor`, `SAFETY_WINDOW_MS`, `MAX_PAGES`, `buildTextQuery`, the three new `jobs` columns, and
`updateJobProgress`. Plan 03-02's `runScrapeJob` can now be written against a real, migrated schema.
</success_criteria>

<output>
Create `.planning/phases/03-job-creation-checkpointed-worker/03-01-SUMMARY.md` when done
</output>
